'use client';import{useState}from'react';import{useRouter}from'next/navigation';import{api}from'@/lib/api';import toast from'react-hot-toast';import{LogIn}from'lucide-react';export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});toast.success('أهلاً وسهلاً');router.push('/admin')}catch(e){toast.error(e instanceof Error?e.message:'تعذّر الدخول')}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="card w-full max-w-md space-y-5 p-8"><p dir="ltr" className="font-bold tracking-widest text-olive">k2nobeit</p><h1 className="text-3xl font-extrabold">دخول لوحة الإدارة</h1><label className="block">البريد الإلكتروني<input required type="email" className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block">كلمة المرور<input required type="password" minLength={8} className="input mt-2" value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={busy} className="btn btn-primary w-full"><LogIn size={19}/>{busy?'جاري الدخول…':'دخول'}</button></form></main>}
